#include<bits/stdc++.h>
using namespace std;
int fibbo(int n)
{
    if (n==0)
    {
        return 0;
    }
    if(n==1)
    {
        return 1;
    }
    else
    {
        return fibbo(n-2)+fibbo(n-1);
    }
}
    
int main()
{
    int m;
    cin>>m;
    cout<<fibbo(m);    
}
